IMPORTANT:
>> Bug fixes:
• Don't allow selling the same UID multiple times in a row

>> Clean-up
• ✅ Clean up heejinSettings.js and split into multiple files
• Cardmanager.toString.setEntry use card global ID instead of card object

UN-IMPORTANT:
>> Commands
• /leaderboard | a per server leaderboard

>> Extra
• show player's start date in /information

>> Personal
• add check to message_embed.send if interaction if null

FUTURE:
>> Commands
• /applycard | update global ids
• remove card(s) from user
• request customs through heejin (uses modals)

UN-RELATED:
>> discordTools.js
• BetterEmbed.new() | returns a new BetterEmbed with the same configuration


FIXES:
added custom cards back

fixed /gift not properly checking if the card's in your inventory
fixed case sensitivity for gifting cards

fixed double xiyeon